import styled from 'styled-components';

export const TaskSolutionStyled = styled.div(() => ({}));

export const TaskSolutionHeader = styled.div(() => ({
  display: 'flex',
  flexDirection: 'row',
}));
