import styled from 'styled-components';

export const TaskViewStyled = styled.div(() => ({
  display: 'flex',
  flexDirection: 'column',
  gap: '10px',
}));

export const TaskAuthorWrapper = styled.div(() => ({
  marginTop: '20px',
  marginBottom: '10px',
}));
